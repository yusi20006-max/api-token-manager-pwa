name: Phase 6.3 Backup Restore Test

on:
  push:
    branches:
      - fix/phase6-3-lossless-backup-restore

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm test
